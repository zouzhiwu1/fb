/**
 * football-betting-platform API 根地址（仅 origin，无路径，勿以 / 结尾）。
 * 真机与正式版必须使用 HTTPS，且域名需在小程序后台「服务器域名」中配置 request / downloadFile。
 * 本地调试可在微信开发者工具「详情 → 本地设置」勾选「不校验合法域名」。
 */
const API_BASE = 'https://trybx.cn';

module.exports = {
  API_BASE,
};
